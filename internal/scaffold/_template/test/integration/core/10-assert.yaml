# Expected outcome of the create-instance step.
#
# Assert both the accepted Instance spec and the operator-native CR the
# provider must create from it. Chainsaw asserts are partial: only the
# fields listed here are checked.
apiVersion: core.openeverest.io/v1alpha1
kind: Instance
metadata:
  name: test-instance
spec:
  providerRef:
    name: [[ .ProviderName ]]
# TODO: assert the operator-native CR produced by Sync(), e.g.:
# ---
# apiVersion: <operator-api-group>/v1
# kind: <OperatorKind>
# metadata:
#   name: test-instance
# spec:
#   replicas: 1
#   ...
